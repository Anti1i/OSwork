/*************************************************************************//**
 *****************************************************************************
 * @file   zuc.c
 * @brief  ZUC Stream Cipher Implementation (祖冲之算法)
 * @author Orange'S Development Team
 * @date   2026
 *
 * References:
 * - ZUC Specification v1.6 (2011)
 * - 3GPP TS 35.222 V12.0.0
 *****************************************************************************
 *****************************************************************************/

#include "type.h"
#include "zuc.h"
#include "string.h"

/* S-boxes for ZUC algorithm */
static const unsigned char S0[256] = {
    
};

static const unsigned char S1[256] = {
    
};

/* D constant for LFSR initialization */
static const unsigned char D[16] = {
    0x44, 0x5F, 0x73, 0x68, 0x5C, 0xC2, 0xFC, 0x9F,
    0x4D, 0x79, 0x76, 0x6C, 0x53, 0xBB, 0xF1, 0xA8
};

/* Helper functions */
#define ROT(x, n) (((x) << (n)) | ((x) >> (32 - (n))))

/* Linear transformation L1 */
static unsigned int L1(unsigned int X) {
    return X ^ ROT(X, 2) ^ ROT(X, 10) ^ ROT(X, 18) ^ ROT(X, 24);
}

/* Linear transformation L2 */
static unsigned int L2(unsigned int X) {
    return X ^ ROT(X, 8) ^ ROT(X, 14) ^ ROT(X, 22) ^ ROT(X, 30);
}

/* Non-linear function F (simplified) */
static unsigned int F(unsigned int X0, unsigned int X1, unsigned int X2,
                      unsigned int* R1, unsigned int* R2) {
    unsigned int W, W1, W2;
    unsigned int u, v;

    W = (X0 ^ *R1) + *R2;
    W1 = *R1 + X1;
    W2 = *R2 ^ X2;

    /* S-box substitution on W1 */
    u = ((unsigned int)S0[(W1 >> 24) & 0xFF] << 24) |
        ((unsigned int)S1[(W1 >> 16) & 0xFF] << 16) |
        ((unsigned int)S0[(W1 >> 8) & 0xFF] << 8) |
        ((unsigned int)S1[W1 & 0xFF]);

    v = L1(u);

    /* Update R1 and R2 */
    *R1 = S0[(W2 >> 24) & 0xFF] << 24 |
          S1[(W2 >> 16) & 0xFF] << 16 |
          S0[(W2 >> 8) & 0xFF] << 8 |
          S1[W2 & 0xFF];
    *R1 = L2(*R1);

    *R2 = v;

    return W;
}

/* LFSR with initialization mode */
static void LFSRWithInitMode(unsigned int* LFSR, unsigned int u) {
    unsigned int s16;
    unsigned int i;

    s16 = LFSR[0];

    /* Calculate feedback */
    s16 = (s16 << 8) ^
          (((LFSR[0] << 20) ^ (LFSR[4] << 8) ^ (LFSR[10] << 21) ^
            (LFSR[13] << 17) ^ (LFSR[15] << 15)) >> 1);

    s16 = (s16 + u) & 0x7FFFFFFF;

    /* Shift LFSR */
    for (i = 0; i < 15; i++) {
        LFSR[i] = LFSR[i + 1];
    }
    LFSR[15] = s16;
}

/* LFSR with work mode */
static void LFSRWithWorkMode(unsigned int* LFSR) {
    unsigned int s16;
    unsigned int i;

    s16 = LFSR[0];

    /* Calculate feedback */
    s16 = (s16 << 8) ^
          (((LFSR[0] << 20) ^ (LFSR[4] << 8) ^ (LFSR[10] << 21) ^
            (LFSR[13] << 17) ^ (LFSR[15] << 15)) >> 1);

    s16 = s16 & 0x7FFFFFFF;

    /* Shift LFSR */
    for (i = 0; i < 15; i++) {
        LFSR[i] = LFSR[i + 1];
    }
    LFSR[15] = s16;
}

/* Bit reorganization */
static void BitReorganization(unsigned int* LFSR, unsigned int* X) {
    X[0] = ((LFSR[15] & 0x7FFF8000) << 1) | (LFSR[14] & 0xFFFF);
    X[1] = ((LFSR[11] & 0xFFFF) << 16) | (LFSR[9] >> 15);
    X[2] = ((LFSR[7] & 0xFFFF) << 16) | (LFSR[5] >> 15);
    X[3] = ((LFSR[2] & 0xFFFF) << 16) | (LFSR[0] >> 15);
}

/**
 * @brief Initialize ZUC state with key and IV
 */
PUBLIC void zuc_init(zuc_state_t* state, const unsigned char* key, const unsigned char* iv) {
    int i;
    unsigned int X[4];
    unsigned int w;

    /* Load key and IV into LFSR */
    for (i = 0; i < 16; i++) {
        state->LFSR[i] = ((unsigned int)key[i] << 23) |
                         ((unsigned int)D[i] << 8) |
                         (unsigned int)iv[i];
        state->key[i] = key[i];
        state->iv[i] = iv[i];
    }

    /* Initialize R1 and R2 */
    state->R1 = 0;
    state->R2 = 0;

    /* Initialization stage (32 rounds) */
    for (i = 0; i < 32; i++) {
        BitReorganization(state->LFSR, X);
        w = F(X[0], X[1], X[2], &state->R1, &state->R2);
        LFSRWithInitMode(state->LFSR, w >> 1);
    }

    /* Discard first keystream word */
    BitReorganization(state->LFSR, X);
    F(X[0], X[1], X[2], &state->R1, &state->R2);
    LFSRWithWorkMode(state->LFSR);

    state->initialized = 1;
}

/**
 * @brief Generate keystream words
 */
PUBLIC void zuc_generate_keystream(zuc_state_t* state, unsigned int* keystream, int num_words) {
    int i;
    unsigned int X[4];

    if (!state->initialized) {
        return;
    }

    for (i = 0; i < num_words; i++) {
        BitReorganization(state->LFSR, X);
        keystream[i] = F(X[0], X[1], X[2], &state->R1, &state->R2) ^ X[3];
        LFSRWithWorkMode(state->LFSR);
    }
}

/**
 * @brief Encrypt/Decrypt data using ZUC
 */
PUBLIC void zuc_crypt(zuc_state_t* state, const unsigned char* input,
                      unsigned char* output, int length) {
    int i, j;
    int num_words = (length + 3) / 4;
    unsigned int keystream[256]; /* Max 1KB at a time */

    if (!state->initialized) {
        return;
    }

    /* Process in chunks */
    while (length > 0) {
        int chunk_size = (length < 1024) ? length : 1024;
        int chunk_words = (chunk_size + 3) / 4;

        /* Generate keystream */
        zuc_generate_keystream(state, keystream, chunk_words);

        /* XOR with keystream */
        for (i = 0; i < chunk_size; i++) {
            int word_idx = i / 4;
            int byte_idx = i % 4;
            unsigned char ks_byte = (keystream[word_idx] >> (24 - byte_idx * 8)) & 0xFF;
            output[i] = input[i] ^ ks_byte;
        }

        input += chunk_size;
        output += chunk_size;
        length -= chunk_size;
    }
}

/**
 * @brief Initialize ZUC with password (simplified key derivation)
 */
PUBLIC void zuc_init_with_password(zuc_state_t* state, const char* password, int pass_len) {
    unsigned char key[16];
    unsigned char iv[16];
    int i;
    unsigned int hash = 0x5A5A5A5A;

    /* Simple key derivation from password */
    /* This is NOT cryptographically secure - just for demo */
    for (i = 0; i < pass_len; i++) {
        hash = hash * 31 + (unsigned char)password[i];
        hash = (hash << 13) | (hash >> 19); /* Rotate */
    }

    /* Generate key */
    for (i = 0; i < 16; i++) {
        hash = hash * 1103515245 + 12345;
        key[i] = (unsigned char)(hash >> 16);
    }

    /* Generate IV */
    hash = hash ^ 0xA5A5A5A5;
    for (i = 0; i < 16; i++) {
        hash = hash * 1103515245 + 12345;
        iv[i] = (unsigned char)(hash >> 16);
    }

    /* Initialize with derived key and IV */
    zuc_init(state, key, iv);
}
